# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# source data example generator
source_generator*
draft*

# The explorer page is a build artefact: CI generates it and publishes it, so a
# committed copy could only ever be stale. Generate it locally to browse it:
#     uv run python -m afbstepomop.explorer docs/index.html
docs/index.html
afbstep_explorer.html
